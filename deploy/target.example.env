# Where the deploy and audit scripts point. Copy to deploy/.target.env, which
# is gitignored, and fill in your own environment.
#
#   cp deploy/target.example.env deploy/.target.env
#
DEPLOY_HOST=root@your-server.example.com
PORTAL_BASE=https://privacy.example.com
